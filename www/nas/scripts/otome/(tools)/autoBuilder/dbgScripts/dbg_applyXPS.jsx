/*	applyXPS()
 *	コンポとレイヤのメソッドとして実装する?(案）
 *	コンポのメソッドは
		コンポとしてXPSとの関連付けを記録照合
		＋自己のレイヤコレクションの同名メソッドを逐次実行する
 *	レイヤメソッドとしては
		自分自身と関連付けられたタイムラインの管理と照合を行う
 *	
 *	
 *	
 *	
 */

if(app.project.activeItem){app.project.activeItem.applyXPS(XPS)}

