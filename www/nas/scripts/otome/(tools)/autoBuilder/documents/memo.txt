作業メモ
GUI ライブラリのリストボックスとドロップダウンボックスを新GUIパーツに対応
8以降はそちらですませる
書式は無視して速い方

おまぽんの　判定とビルド手順を書き換えて今回のプロジェクトに無理くり対応させる今週中

テンプレートプロジェクトを作る　000番でおっけ

９・１５

思い出した　リストボックスはアクセスメソッドの互換がないので単純置き換えは無理



なので今回はパス


xUIオブジェクトはＡＥではぜんぜん関係ないので、生き残っているコードを掃除すること

ロックされたレイヤがあると、不都合がでるのでロックされたレイヤを検出して処理をスキップすること
解除して操作するのは要注意　危険危険　ロックは意図的なモノが多いので、すっ飛ばすのを基本方針で


インタフェースなしのコンポビルダ

指定されたXPSオブジェクトのコンポを組み上げてタイムラインを適用するルーチン

XPSの指定であることがポイント

コードの流用はあるが、とりあえず別プロシジャ

オプションで背景の指定は可能にする。(現状のXPSで背景の指定が明確でないため)
(この辺無視して背景は手動調整って手もある…)

案として
	「背景らしいフッテージは全部取り込み」(いまこれ)
	「背景らしいフッテージは全部無視」
	「背景らしいフッテージのうち一個だけ一番下に敷く」
どれかな？全部か？


とりあえず乙女のメソッド

nas.otome.buildStage(myXps)
{
//例によってmyXpsがなければXPSを使用
//背景処理部分
/*
ステージコンポサイズの設定は、依然と同様で設定値により
	入力ライブラリサイズ(+マージン)
	フッテージサイズ(＋マージン)
	指定サイズ
*/
}


またはXPSの拡張メソッドか？
XPS.buildStage(myItems,myOption)
{
}

カメラステージは、CompItemの拡張メソッドにするべき
